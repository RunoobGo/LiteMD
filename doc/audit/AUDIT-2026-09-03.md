# LiteMD 全景审计报告（阶段一）

> 审计日期：2026-09-03
> 审计基线：本地 main @ `e37818e`（v0.2.9），工作区干净（仅一个未跟踪的审查报告 HTML）
> 审计范围：doc/ 全部文档 + README + Go 后端全量 + 前端 TS 全量 + 构建脚本
> 测试基线：Go 4 包全绿（69 个 Test 函数）；前端 10/10 套件全绿

---

## 一、问题清单（静态代码审计）

> 结论先行：**未发现新的 P0 级隐患**。前两轮审查（v0.2.3、v0.2.9 前的 CODE-REVIEW-2026-09-02）已清盘 7×P0 + 13×P1 + 14×P2，本轮静态审计重点复核了 IPC 面（17 个绑定全部有输入校验或路径守卫）、并发（ctxMu/unsaved/startupFileQueue 均正确加锁）、资源泄漏（监听器移除、pointercancel、LRU 上限均已处理），仅余以下小项：

| 编号 | 级别 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| S1 | P1 | [main.ts:988](../../frontend/src/main.ts#L988)、[:526](../../frontend/src/main.ts#L526)、[:528](../../frontend/src/main.ts#L528) | 前端用 `e.message.includes("modified by another program")` 等**错误文案字符串匹配**做分支。Go 侧改一个字，保存冲突检测就静默退化为普通错误弹窗（不丢数据，故 P1 非 P0）。 | Go 侧补错误文案稳定性契约测试；契约写入 TECHNICAL.md |
| S2 | P1 | [frontend/package.json](../../frontend/package.json) | `dompurify ^3.4.13`，低于 TECHNICAL.md 待办 P1 指定的 3.4.14（安全依赖） | 升级 + 回归 |
| S3 | P2 | [links.go:262](../../internal/links/links.go#L262) | `normalizePath` 死代码：仅测试引用，无生产调用方 | 连同测试删除 |
| S4 | P2 | [open.go:68](../../internal/links/open.go#L68) | `requireExistingFile` 死代码：生产路径已全部改用 `statExistingFile`，仅测试引用 | 连同测试删除 |
| S5 | P2 | [file-ops.ts:125](../../frontend/src/file-ops.ts#L125) | `window.__litemd__bindings` E2E 接缝缺 `SetUnsavedCount` / `SaveDialog` / `ConsumeStartupFile` / `AppInfo` | 补齐导出 |
| S6 | P2 | [main.go:38](../../main.go#L38) | navGuard 用 `httptest.NewRecorder` 全量缓冲每个 GET 响应再拷贝（主 chunk ~1MB 双份内存）。本地 assetserver 无实测瓶颈，按「禁止盲目重构」约束**仅记录不动** | 已知限制 |
| S7 | P2 | [app.go:340-343](../../app.go#L340-L343) | `takeCtxForNotify` 的文档注释重复粘贴了两遍 | 去重 |
| S8 | P2 | [app.go:454](../../app.go#L454) | `SetConfig` 绑定无前端消费方（file-ops.ts 只包装了 `GetConfig`，也无人调用）。TECHNICAL.md §5.2 已如实记录该现状，属已知设计决策 | 保留，文档已对齐 |

**复核通过项（无需动作）**：硬编码检查——`AppVersion` 为唯一事实源且构建脚本六处同步；配置路径走 `~/.litemd/`；无魔数裸奔（防抖分档、缓存容量均有注释论证）。错误处理——Go 侧无被吞的 error；前端 `catch` 均有用户可见提示或明确注释说明静默理由。内存——监听器均 `{ once: true }` 或显式移除；`saveLocks` Map 以 tab 生命周期为界（tab 关闭后残留一条已 settle 的 Promise，量级恒定，可忽略）。IPC 安全——`OpenFile` 有类型白名单+凭据黑名单，`SaveFile` 有 mtime 冲突检测，`CopyImageAsset` 写入位置由后端推导，`OpenExternal` 有 scheme 白名单，`OpenPath` 有可执行黑名单+常规文件校验。

---

## 二、差异报告（文档 vs 代码对齐检查）

### A. 文档有、代码未实现（或文档过时）

| 编号 | 级别 | 文档位置 | 差异内容 |
|---|---|---|---|
| A1 | **P1** | CHANGELOG v0.2.0 | 声称「自动更新检查」（GitHub Releases API、semver 比较、24h 节流、资产选择）及「Go 单测 4 包 app/config/fileio/updater」。**updater 已于 2026-08-31 e64af52 提交整体移除**，CHANGELOG 从未记录移除。README 未声称该功能（与代码一致）。 |
| A2 | **P1** | CHANGELOG v0.2.0 | 声称「命令面板 (Ctrl+P)」与「多种 keyMap（默认/Vim/Emacs）」。现状：Ctrl+P 是切换预览模式；editor.ts 无 Vim/Emacs keymap。README 快捷键表已与代码一致，仅 CHANGELOG 历史条目失真。 |
| A3 | **P1** | TECHNICAL.md §8.1 | 「Mermaid 图表渲染未实现（P1 候选）」——与自身 §3.3、CHANGELOG v0.2.8 直接矛盾（已实现且有 22+ 断言）。 |
| A4 | **P1** | TECHNICAL.md §8.2 | P1 待办表三项已修仍列待办：OnBeforeClose（v0.2.9 已修）、PushRecent 事务化（Mutate 已修）、保存重构（updateContentBaseline+enqueueSave 已修）。macOS OnFileOpen 仍未做（真实待办，应保留）。 |
| A5 | P2 | TECHNICAL.md 头部/§6.1/§7.2 | 头部标注「对齐 v0.2.2」（现 v0.2.9）；产物示例文件名 0.2.1；测试统计停留在 v0.2.7（8 套件，现为 10 套件 + Go 69 Test 函数）。 |
| A6 | P2 | TECHNICAL.md §2 vs §8.2 | e2e 有效集两处说法互相矛盾（§2 说 sprint4/6/7/8/9 有效、§8.2 说要修 sprint1/2/4/5），且均与现状（sprint4-11 有效）不符。 |
| A7 | P2 | TECHNICAL.md §8.2 | 待办「DOMPurify 升 3.4.14」，实际 ^3.4.13（=问题 S2）。 |

### B. 代码有、文档未记录

| 编号 | 级别 | 内容 |
|---|---|---|
| B1 | **P1** | **CHANGELOG v0.2.9 段遗漏多项已落地修复**（对照 git log 逐条核实）：P0-2 CopyImageAsset 收敛为受限资产写入（ab56c77）；P0-5 SaveFile 外部修改冲突检测 + 前端保存串行化（7b62c80）；P0-1/P1-6 mermaid 失败原因三分/渲染超时/串行队列（09d43b8）；editor search() 扩展补装 + 切标签清空 undo 栈（059d792）；P1-8 pendingNotify 并发锁修复（f6c55da 内）；P1-12/P2 构建脚本 VERSION 语义拆分与三处测试硬伤修复（9a3d777）。其中两项 **P0 级安全修复未进 CHANGELOG**，破坏可追溯性。 |
| B2 | P2 | 测试套件隔离运行器（LITEMD_SUITE 父/子进程模型）未写入 TECHNICAL.md §7.1。 |
| B3 | P2 | `SetUnsavedCount` 的 mock 侧行为（`__litemd__unsavedCount`）与 `__litemd__bindings` E2E 接缝未记录于 TECHNICAL.md。 |

---

## 三、分级测试计划

> 优先级定义：**P0** 核心业务流程（不通过即阻塞发版）；**P1** 边界条件与异常处理；**P2** UI/UX 细节。
> 标注 🟢 = 已有自动化覆盖（含位置）；🟡 = 部分覆盖需补强；🔴 = 无覆盖需新增。

### 模块 1：文件打开/保存管线（P0）

| 用例 | 优先级 | 状态 |
|---|---|---|
| OpenFile：正常 .md 读取，返回 abs 路径/内容/mtime | P0 | 🟢 app_test.go |
| OpenFile：文本白名单（.exe 拒绝）、凭据黑名单（id_rsa/.env/.pem） | P0 | 🟢 links_test.go |
| ReadText：不存在/超 50MB/无效 UTF-8/含 NUL/FIFO 设备 | P0 | 🟢 fileio_test.go |
| SaveFile：expectMtime 不符 → ErrExternalModified，不写入 | P0 | 🟢 app_test.go |
| SaveFile：expectMtime=0 强制覆盖（冲突确认路径） | P0 | 🟢（Go 侧）；🟡 前端 confirmOverwrite 流程无单测 |
| 原子写：临时文件→fsync→rename；目标已存在沿用原权限 | P0 | 🟢 fileio_test.go |
| 保存期间继续输入不丢字（enqueueSave 串行 + effectiveContent 执行期取值） | P0 | 🔴 前端无单测（E2E sprint 覆盖部分） |
| SaveFileAs：取消返回空；无扩展名补 .md | P1 | 🟢 app_test.go |

### 模块 2：标签管理（P0）

| 用例 | 优先级 | 状态 |
|---|---|---|
| newTab/openTab/activate/closeTab 生命周期与 dirty 翻转 | P0 | 🟢 tabs.test.ts |
| 路径去重：重复打开同一路径激活旧标签 | P0 | 🟢 tabs.test.ts |
| 空未保存标签被 openInCurrentIfEmpty 覆盖（不新增标签） | P0 | 🟡 逻辑在 main.ts，E2E 覆盖 |
| 关闭最后一个标签自动新建空标签 | P0 | 🟢 tabs.test.ts（+项目硬约束） |
| 切标签光标/滚动位置记忆与恢复 | P1 | 🟡 E2E 部分覆盖 |
| 切标签清空 undo 栈（防跨文件污染） | P0 | 🟡 editor 行为，无直接断言 |

### 模块 3：渲染管线（P0）

| 用例 | 优先级 | 状态 |
|---|---|---|
| XSS 五层防护（script/javascript:/onerror/iframe/onclick 载荷） | P0 | 🟢 preview.test.ts |
| Obsidian 双链/callout/frontmatter（含 CRLF、行号守恒） | P0 | 🟢 obsidian.test.ts |
| LaTeX 占位符防伪 + 代码块豁免 + ReDoS 守卫 | P0 | 🟢 latex.test.ts |
| 内嵌 CSS 作用域隔离（五类攻击面） | P0 | 🟢 user-css.test.ts |
| Mermaid：缓存/主题隔离/错误三分/超时/串行 | P0 | 🟢 mermaid.test.ts |
| 大文档分块缓存与整文输出等价性 | P1 | 🟢 preview.test.ts（增量渲染） |
| data URI 图片 MIME 白名单（拒 svg+xml） | P1 | 🟢 preview.test.ts |

### 模块 4：链接与本地资源（P0）

| 用例 | 优先级 | 状态 |
|---|---|---|
| classifyHref：external/mail/anchor/local/unsafe 分类 + 控制字符剥离 | P0 | 🟢 link-handler.test.ts |
| Resolve：file:// 剥离/百分号解码/盘符/UNC/../折叠/无扩展名嗅探 | P0 | 🟢 links_test.go |
| OpenExternal：scheme 白名单（拒 file://、javascript:） | P0 | 🟢 links_test.go |
| OpenWithSystem：可执行扩展黑名单 + 非常规文件拒绝 | P0 | 🟢 links_test.go |
| ReadAssetDataURL：TOCTOU 防护/10MB 上限/.svg 默认拒绝 | P0 | 🟢 links_test.go |
| navGuard 兜底：404→302 /?nav=，/wails/ 与非 GET 透传 | P0 | 🟢 navguard_test.go |

### 模块 5：未保存守卫（P0）

| 用例 | 优先级 | 状态 |
|---|---|---|
| dialog returnValue 残留清零（ESC 不误执行上次选择） | P0 | 🟢 unsaved-guard 相关断言 |
| SetUnsavedCount 上报 + beforeClose 放行/否决/负数钳位 | P0 | 🟢 app_close_test.go |
| pendingNotify 并发补发（二实例打开不丢事件） | P0 | 🟢 app_notify_test.go |

### 模块 6：单实例与启动文件（P1）

| 用例 | 优先级 | 状态 |
|---|---|---|
| extractStartupFiles：旗标跳过/相对路径规范化/仅常规文件 | P1 | 🟢 startupfile_test.go |
| 队列并发 push/pop | P1 | 🟢 startupfile_test.go |
| macOS Finder 双击打开（OnFileOpen 未配置，已知限制） | P1 | 🔴 已知失效，文档记录 |

### 模块 7：配置持久化（P1）

| 用例 | 优先级 | 状态 |
|---|---|---|
| 损坏 config.json 自愈（ErrCorrupted 降级 + Mutate 覆盖落盘） | P1 | 🟢 config_test.go |
| PushRecent 去重/上限 10/Mutate 事务化 | P1 | 🟢 config_test.go + app_test.go |

### 模块 8：UI/UX（P2）

| 用例 | 优先级 | 状态 |
|---|---|---|
| 标签栏/TOC/分屏把手键盘可达性 | P2 | 🟡 E2E sprint7/8 覆盖 |
| 标题栏拖动/双击最大化/最大化图标同步 | P2 | 🟢 titlebar.test.ts |
| 主题切换（明暗）/mermaid 主题跟随 | P2 | 🟢 mermaid.test.ts |
| toast 浮层/navGuard 提示 | P2 | 🟡 E2E sprint9 |

### 测试覆盖结论

核心业务逻辑（Go 侧模块 1/4/5/7 + 前端渲染/标签）自动化覆盖率估计 **>85%**，满足 >80% 目标。缺口集中在 **main.ts 编排层**（保存竞态、空标签覆盖、冲突确认流程）——该层依赖 DOM 与 Wails binding，适合 E2E 而非单测；既有 e2e sprint4-11 已覆盖大部分，无阻塞发版的空洞。

---

## 四、阶段二修复计划（按优先级）

| 序 | 对应问题 | 修复项 | 验证 | 状态 |
|---|---|---|---|---|
| 1 | B1+A1 | CHANGELOG v0.2.9 段补记遗漏的 P0-2/P0-5/mermaid 等修复；补记 updater 移除（勘误） | 人工 diff git log ↔ CHANGELOG | ✅ 完成 |
| 2 | A3/A4/A5/A6/B2/B3 | TECHNICAL.md 同步至 v0.2.9 现状（mermaid 待办矛盾、P1 待办表更新、版本头、测试统计、e2e 有效集、运行器说明） | 人工核对 | ✅ 完成 |
| 3 | S1 | Go 侧错误文案契约测试（钉死三个前端依赖的 sentinel 文案）+ 契约写入 TECHNICAL.md | go test | ✅ 通过 |
| 4 | S2/A7 | DOMPurify 升 3.4.14 | 前端全量测试 | ✅ 通过 |
| 5 | S3/S4 | 删除死代码 normalizePath/requireExistingFile 及其测试 | go test | ✅ 通过 |
| 6 | S5 | __litemd__bindings 补齐缺失导出 | 前端构建 + tsc | ✅ 通过 |
| 7 | S7 | app.go 重复注释去重 | go build | ✅ 通过 |

S6（navGuard 缓冲）按约束条件 2 不做重构，仅在 TECHNICAL.md 已知限制中记录。

**阶段二最终验证**（2026-09-03）：
- `go build ./...` 通过；`go test ./... -race -count=1` 4 包全绿（含新增 `TestErrorTextContractForFrontend` 与重命名的 `TestNonRegularFileRejected`）
- `npx tsc --noEmit` 无错误
- 前端 `LITEMD_TEST=all` 10/10 套件、26 断言全绿（DOMPurify 3.4.14 下）
- 修复记录已同步至 CHANGELOG.md「[未发布]」段

---

*审计方法：全量阅读 Go 后端（app/main/startupfile/internal 三包）与前端主干（main/tabs/editor/file-ops/preview 核心 + 依赖版本核对），文档侧逐条对照 CHANGELOG 0.1.0-0.2.9、TECHNICAL.md、README；git 历史逐 commit 核对修复记录完整性。*
