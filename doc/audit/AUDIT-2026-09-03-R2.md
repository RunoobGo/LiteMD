# LiteMD 全景审计报告（第二轮 · 2026-09-03）

> 审计日期：2026-09-03（晚）
> 审计基线：本地 main @ `a695760`（v0.2.9 文档收尾提交），工作区干净
> 第一轮：见 [AUDIT-2026-09-03.md](./AUDIT-2026-09-03.md)，7 项 P1/S1-S7 全部已落地（9af9704）
> ROADMAP：见 [ROADMAP-2026-09-03.md](../design/ROADMAP-2026-09-03.md)，R1-R8 已 ✅，本轮审计覆盖范围 = R1-R8 落地后的新代码（729e1b7、207207f、e5028c9、7a664f7 等）
> 测试基线：Go 4 包全绿（69 Test 函数）；前端 11 套件 410+ 断言全绿
> **一页结论**：见 [AUDIT-SUMMARY-2026-09-03-R2.md](./AUDIT-SUMMARY-2026-09-03-R2.md)

---

## 一、问题清单（静态代码审计 · 第二轮）

> 结论先行：**未发现新的 P0 级隐患**。本轮重点审计 R1-R8 落地后的新代码（macOS OnFileOpen、字号持久化、LaTeX 闸门、拆包、CI、E2E 降级）以及第一轮未触及的 main.ts 编排层。共发现 **P1×9 + P2×18**。

### A. 后端 Go 静态审计

| 编号 | 级别 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| G1 | **P1** | [internal/fileio/fileio.go:57,64,67,80,85,95](../../internal/fileio/fileio.go#L57) | `fmt.Errorf("%w: %s", Err…, path)` 把**完整绝对路径**写入错误串，Wails 序列化到前端后用户可见。对 `~/.ssh/id_rsa` 等凭据路径形成 PII 泄露面 | 用 `filepath.Base(path)` 作为用户可见消息；完整路径走 `log.Printf` |
| G2 | **P1** | [internal/links/asset.go:39](../../internal/links/asset.go#L39) | `var AllowSVG = false` 裸全局可变，测试侧 `links_test.go:252` 写它未加锁。`ReadAssetDataURL` 读取侧同样无锁。`-race` 未发现 = 因无并发用例 | 改 `atomic.Bool` 或收敛进 `Store` 配置 |
| G3 | **P1** | [app_test.go:TestOnFileOpen_PushAndNotify](../../app_test.go) | 误测：`badPath` 声明但**未落盘**，`os.Stat` 走 ENOENT 后由存在性过滤掉，断言名"非 Markdown 后缀拒"实际未验证扩展名拦截。Finder 右键 → "打开方式" 选 `.exe` 会被入队 | 在 `extractStartupFiles` 加扩展名白名单（与 R5 测试意图对齐） |
| G4 | **P1** | [app.go:419](../../app.go#L419) | `wailsruntime.BrowserOpenURL(ctx, u)` 返回值丢弃，未注册协议/沙箱拒绝时用户无感知 | 捕获并向上抛，触发已有 `WailsEmitError` |
| G5 | **P1** | [app.go:241-246](../../app.go#L241) | `SaveFile` expectMtime 路径：`os.Stat` 任意错误（非 IsNotExist）都静默 fallthrough 写盘。EIO/permission 场景数据无提示丢失 | 返回 `fmt.Errorf("stat before save: %w", statErr)`，触发 P0-5 冲突检测流程 |
| G6 | P1 | [internal/links/links.go:142-144](../../internal/links/links.go#L142) | `decodePath` 失败时静默回退原文，`Resolve` 下游 `KindMissing` 提示无"URL 损坏"线索 | 引入 `ErrMalformedPath`，前端可提示"链接解析失败" |
| G7 | P1 | [app_test.go](../../app_test.go) | `CopyImageAsset`（`app.go:501-513`）**0 测试覆盖**。该 binding 写入攻击者可控 base64，应等同 `WriteBase64File` 覆盖：happy / base64 损坏 / 超 20MB / `assetName` 路径穿越 | 补 ≥4 单元测试 |
| G8 | P1 | [app_test.go](../../app_test.go) | `SaveFile` expectMtime 冲突路径（P0-5 落地的关键）**0 直接断言**。`TestErrorTextContractForFrontend` 仅 pin 文案，runtime 行为无回归守护 | 补 1 测试：mtime 不符 → `ErrExternalModified`，文件未动 |
| G9 | P2 | [app.go:160, 239, 199, 214, 417, 503](../../app.go) | 6 处错误文案裸 `errors.New(...)`：`"path is empty"` / `"app not ready"` × 3 / `"base64 data is empty"`。前端 `mocks.ts` 与 `main.ts` 共 ~10 处用 `e.message.includes(...)` 依赖这些字符串 | 替换为 `var ErrXxx = errors.New(...)` 哨兵；契约测试钉死；为后续错误码化铺路 |
| G10 | P2 | [app.go:201-207](../../app.go#L201) | `OpenDialog` 过滤含 `"All Files (*.*)"`，可绕过 UI 选 `/etc/passwd`（被 `CheckEditable` 兜住但 affordance 错误） | 移除 All-Files 项或加 secondary 标签 |
| G11 | P2 | [app.go:79-81](../../app.go#L79) | `shutdown` 钩子体为空（`OnShutdown` 占位） | 删 `OnShutdown` 注册或加 TODO 注明用途 |
| G12 | P2 | [main.go:39](../../main.go#L39) | `navGuard` 用 `httptest.NewRecorder` 全量缓冲（主 chunk 双份内存）。无实测瓶颈，R11 已说明按"无瓶颈不重构"保留 | 维持现状；TECHNICAL §8.1 已记录 |
| G13 | P2 | [app.go:24](../../app.go#L24) | `AppVersion = "0.2.9"` 常量散落（与 wails.json 6 源同步） | 维持现状；VERSION 脚本三模式（9a3d777）已守护 |
| G14 | P2 | [fileio/asset.go:50](../../fileio/asset.go#L50) | 资产名长度 128 裸字面量 | 提 `const MaxAssetNameLen` |
| G15 | P2 | [app.go:463](../../app.go#L463) | `PushRecent(c, path, 10)` 上限 10 与 `config.go:177` 默认重复字面量 | 提 `const MaxRecentFiles = 10` 共享 |

### B. 前端 TS 静态审计

| 编号 | 级别 | 位置 | 问题 | 建议 |
|---|---|---|---|---|
| F1 | **P1** | [main.ts:559,561,630,636,639,1024](../../frontend/src/main.ts) + [mocks.ts:117,162,171,221,224](../../frontend/src/mocks.ts) | **~10 处 `e.message.includes("...")` 文案匹配**，第一轮 S1 已 pin Go 侧契约但前端**仍未切到错误码**。任何 Go 侧文案微调都让保存冲突 / NUL / 凭据三个 fallback UX 静默退化为生硬弹窗 | 引入错误码体系：`FileError` 枚举 + Go 侧返回结构 `{code, message}`，前端 switch 替代 includes |
| F2 | **P1** | [latex.ts:106](../../frontend/src/latex.ts#L106) | `formulaCache` 容量 500 满时 `clear()` 全清，下一次 render 一次性重算整文，**thundering herd**。`preview.ts` / `mermaid.ts` 都已 LRU 化 | 改成 `delete(oldestKey)` 真正 LRU |
| F3 | **P1** | [preview.ts:761](../../frontend/src/preview.ts#L761) + [mermaid.ts:116-124](../../frontend/src/mermaid.ts#L116) | `holder.innerHTML = result.svg` 完全依赖 mermaid 11 `securityLevel: "strict"`。`scrub()` 只剥 `on*` 与 `javascript:`，`<foreignObject>` 等新载体未覆盖 | SVG 注入前再过 `DOMPurify.sanitize(svg, { USE_PROFILES: { svg, svgFilters } })` |
| F4 | P1 | [titlebar.ts:84-93](../../frontend/src/titlebar.ts#L84) | `scheduleSync` 异步取 `win.isMaximised()` 缺代际序保护。`resize → maximize` 串行时旧调用晚归翻转 `is-maximised` class 闪一下 | 加 `gen++`，旧结果丢弃 |
| F5 | P1 | [vite.config.mjs:43-56](../../frontend/vite.config.mjs#L43) | `manualChunks` 用 `id.includes("codemirror")` 字符串匹配，未来依赖 npm 名含子串会被吞入同一 chunk | 改前缀匹配 `/@codemirror\//` 或上 `vite-plugin-chunk-split` |
| F6 | P1 | [latex.ts:138-149](../../frontend/src/latex.ts#L138) | `isTooHeavy` 闸门覆盖 `\raisebox` / `99999em` / 8KB，**未覆盖** `maxExpand` 攻击面（合法但 4KB 公式嵌 2000 个 `\frac{}{}`）。闸门给"已防御"假象 | 文档化分层或对非信任源降 `maxExpand` 更激进 |
| F7 | P1 | [file-ops.ts:125-138](../../frontend/src/file-ops.ts#L125) | `window.__litemd__bindings` 在**生产构建**也注入，重复 `window.go.main.App`，违背"real binding 总被打包"注释意图 | `if (import.meta.env.DEV)` 守门 |
| F8 | P2 | [mermaid.ts:76-81](../../frontend/src/mermaid.ts#L76) | `renderChain` 累计 promise 链（`.then`/`.catch` 永不截断），千次 render 后 GC 释放困难 | 周期 `renderChain = renderChain.catch(()=>{})` |
| F9 | P2 | [mocks.ts:169](../../frontend/src/mocks.ts#L169) | `s.replace(/\/\.\.\//g, "/../")` **自替自身**，死代码 | 删 |
| F10 | P2 | [latex.ts:136](../../frontend/src/latex.ts#L136) | `HUGE_LENGTH_RE` 中 `cm` 出现两次（拷贝残留） | 去重 |
| F11 | P2 | [latex.ts:154](../../frontend/src/latex.ts#L154) | `errorColor: "#e06c75"` 硬编码（oneDark 暗红），亮主题对比度差 | 读 CSS 变量 `--md-error` |
| F12 | P2 | [main.ts:90-127](../../frontend/src/main.ts#L90) | 首启不读 `matchMedia("(prefers-color-scheme: dark)")`，新用户永远拿默认 | localStorage 为空时回退到系统偏好 |
| F13 | P2 | [obsidian.ts:99-128](../../frontend/src/obsidian.ts#L99) | `CALLOUT_RE` 模块级 `/g` 标志，被 `exec` 循环消费安全但与 `findCalloutTransforms:197` 的局部实例模式不一致 | 改局部实例 |
| F14 | P2 | [main.ts:81,110,685,692,1181](../../frontend/src/main.ts) | 9 处 UX 时长字面量（1200/300/4000/6000）散落 | 提常量 |
| F15 | P2 | [main.ts:959,1052](../../frontend/src/main.ts) | `pushRecent().catch(console.warn)` 静默失败无注释说明意图 | 注释 + 决策（提示/静默） |
| F16 | P2 | [font-size.ts:31-38](../../frontend/src/font-size.ts#L31) | `setFontSize` 不防抖，按住 `Ctrl+=` 触发 OS auto-repeat → 高频 localStorage 写 | 加 200ms 防抖或注释说明 |
| F17 | P2 | [main.ts:341-392](../../frontend/src/main.ts#L341) | `targetTabId` 快照不抗 id 复用（理论极小概率，`Date.now()-counter` 碰撞） | 改用 path/contentHash 校验 |

### C. 文档对齐审计（**本轮最严重组**）

| 编号 | 级别 | 文档位置 | 差异内容 |
|---|---|---|---|
| D1 | **P1** | [TECHNICAL.md §5.3 line 344](../../doc/TECHNICAL.md) | "macOS 注意：`mac.Options.OnFileOpen` 未配置" — **已过时**。`main.go:118-129` 已注册（729e1b7）。3 份文档（TECHNICAL / TEST-MATRIX / AUDIT-R1）三处状态不一致 |
| D2 | **P1** | [doc/TEST-MATRIX.md §4 line 108](../TEST-MATRIX.md) | macOS Finder 双击标 🔴 已知失效 — 已实装，应为 🟢 |
| D3 | **P1** | [doc/audit/AUDIT-2026-09-03.md §三 line 119](./AUDIT-2026-09-03.md) | 同 D2，模块 6 macOS Finder 双击标 🔴 |
| D4 | **P1** | [CHANGELOG.md §[0.2.0] line 507-510](../../CHANGELOG.md) | 仍称"命令面板 (Ctrl+P)"与"多种 keyMap（默认/Vim/Emacs）" — **历史失真**未勘误（第一轮 A2 仅记入 0.2.9 勘误） |
| D5 | P2 | [README.md line 17](../../README.md) | 写"`.md` 文件关联"，实际 5 扩展（md/markdown/mdown/mkd/mkdn） |
| D6 | P2 | [README.md line 40](../../README.md) | 写"`Ctrl+Shift+F` 查找替换" — CodeMirror 默认是 `Mod-H`（`Ctrl+H`），该键无内置动作 |
| D7 | P2 | [TECHNICAL.md §7.2 line 399-414](../../doc/TECHNICAL.md) | 测试统计表 7 处数字偏差 + 漏 `font-size.test.ts` + 漏 sprint10/11 |
| D8 | P2 | [AUDIT-R1 §四 line 160](./AUDIT-2026-09-03.md) | "10/10 套件、26 断言" — 26 非断言总数也非套件数，表述错乱 |
| D9 | P2 | [ROADMAP-2026-09-03.md §一 R7](../../doc/design/ROADMAP-2026-09-03.md) | R7 写"17 用例"，实为 16 |
| D10 | P2 | [CHANGELOG [未发布]](../../CHANGELOG.md) | R4（图片插入转义）随 #9 修复提前落地，但 [未发布] 段未提 |

**复核通过项**：
- 版本号 6 源完全一致（0.2.9）
- IPC 输入校验：17 个绑定全部有白名单 / 路径守卫
- 并发：ctxMu / unsaved / startupFileQueue / pendingNotify 均正确加锁
- 资源：监听器 `{ once: true }` 或显式移除；`saveLocks` 以 tab 生命周期为界
- 安全：OpenFile 白名单+凭据黑名单；SaveFile mtime 冲突；CopyImageAsset 写入位置由后端推导；OpenExternal scheme 白名单；OpenPath 可执行黑名单
- mermaid / latex / obsidian / user-css / data URI 5 层 XSS 防护均已覆盖
- DOMPurify 3.4.14

---

## 二、差异报告（文档 vs 代码 · 第二轮）

### A. 文档有、代码已实装但未同步（**高优先级**）

| 编号 | 级别 | 文档位置 | 应改 |
|---|---|---|---|
| AD1 | P1 | TECHNICAL §5.3 / TEST-MATRIX §4 / AUDIT-R1 §三 | 3 处"macOS OnFileOpen 未配置/失效"统一改为"已实装"并引用 commit 729e1b7 + `app_test.go:TestOnFileOpen_PushAndNotify` |
| AD2 | P1 | CHANGELOG [0.2.0] line 507-510 | "命令面板 (Ctrl+P)" + "多种 keyMap" 两行加 **勘误注脚**指向 v0.2.9 段，或删行并入勘误 |

### B. 文档统计偏差（集中 P2）

| 编号 | 级别 | 文档位置 | 现状 | 应为 |
|---|---|---|---|---|
| AB1 | P2 | TECHNICAL §7.2 测试统计 | 10 套件、preview 84 / latex 55 / mermaid 26 / titlebar 21 / tabs 22 / link-handler 33 / toc 33 / md-escape 30 | 11 套件 + `font-size.test.ts`；preview 91 / latex 59 / mermaid 25 / titlebar 17 / tabs 21 / link-handler 23 / toc 22 / md-escape 9 |
| AB2 | P2 | TECHNICAL §7.2 E2E 行 | "16/29/30/14/26/新增"（sprint4/6/7/8/9 + 新增） | 补 sprint10 (28) + sprint11（mermaid） |
| AB3 | P2 | AUDIT-R1 §四 line 160 | "10/10 套件、26 断言" | "11/11 套件，~410 断言全绿（DOMPurify 3.4.14）" |
| AB4 | P2 | TEST-MATRIX §1 line 22 | "sprint1/2/3/5 历史断言失效" | 改为"sprint1/2/3/5 已降级归档至 `e2e/SPRINT-LEGACY-README.md`" |
| AB5 | P2 | ROADMAP §一 R7 | 17 用例 | 16 用例 |
| AB6 | P2 | CHANGELOG [未发布] | 性能段提 R1/R2，缺 R4 | 加 R4 一行（#9 修复已落地） |

### C. 用户可见功能差异

| 编号 | 级别 | 文档位置 | 现状 | 应为 |
|---|---|---|---|---|
| AC1 | P2 | README 特性行 | 写"`.md` 文件关联" | "`.md`/`.markdown`/`.mdown`/`.mkd`/`.mkdn` 文件关联" |
| AC2 | P2 | README 快捷键 | `Ctrl+Shift+F` 查找替换 | `Ctrl+H` 查找替换（CodeMirror `searchKeymap` 默认） |

---

## 三、分级测试计划（第二版）

> 优先级定义沿用第一轮：**P0** 核心业务流程（阻塞发版）；**P1** 边界与异常；**P2** UI/UX 细节。
> 🟢 = 已有自动化 | 🟡 = 部分覆盖需补强 | 🔴 = 无覆盖

### 模块 1：文件打开/保存（P0）

| 用例 | 级别 | 状态 | 缺口/动作 |
|---|---|---|---|
| OpenFile：白名单 + 凭据黑名单 | P0 | 🟢 | OK |
| ReadText：不存在/超 50MB/无效 UTF-8/NUL/FIFO | P0 | 🟢 | OK |
| SaveFile 原子写 | P0 | 🟢 | OK |
| **SaveFile expectMtime 冲突（P0-5 runtime）** | **P0** | **🔴 G8** | **补 `TestAppSaveFile_ExpectMtimeMismatch_Rejects` + 文件未改动断言** |
| SaveFile expectMtime=0 强制覆盖 | P1 | 🟡 | confirmOverwrite JS 流程缺单测（E2E 部分） |
| 保存期间继续输入不丢字 | P0 | 🟡 | enqueueSave 串行 + effectiveContent 执行期取值，前端无单测 |
| **CopyImageAsset happy / 坏 base64 / 超 20MB / 路径穿越** | **P0** | **🔴 G7** | **补 ≥4 单元测试**（与 WriteBase64File 对齐） |
| **extractStartupFiles 拒非 Markdown 扩展名** | **P0** | **🔴 G3** | **补真实 .exe 文件落盘 + 断言不入队**（修正误导测试） |

### 模块 2：标签管理（P0）

| 用例 | 级别 | 状态 |
|---|---|---|
| newTab/close/activate + dirty 翻转 | P0 | 🟢 tabs.test.ts |
| 路径去重 | P0 | 🟢 |
| **空未保存标签被 openInCurrentIfEmpty 覆盖** | **P0** | **🔴** main.ts 编排无单测 |
| 关闭最后标签自动新建 | P0 | 🟢（断言无活动 + 结构性） |
| 切标签光标/滚动位置记忆 | P1 | 🟡 E2E sprint7/8 |
| **切标签清空 undo 栈（防跨文件污染）** | **P0** | **🔴** editor.ts:54,151-157 无断言 |

### 模块 3：渲染管线（P0）

| 用例 | 级别 | 状态 |
|---|---|---|
| XSS 五层防护 | P0 | 🟢 preview.test.ts |
| Obsidian 双链/callout/frontmatter | P0 | 🟢 obsidian.test.ts |
| LaTeX 占位符防伪 + 代码块豁免 + ReDoS | P0 | 🟢 latex.test.ts |
| **LaTeX 闸门（F6 层 maxExpand 攻击面）** | P1 | **🟡 F6** 文档化分层 |
| LaTeX 闸门（recently added） | P0 | 🟢 latex.test.ts (8 cases) |
| 内嵌 CSS 作用域隔离 | P0 | 🟢 user-css.test.ts |
| Mermaid：缓存/主题/三分/超时/串行 | P0 | 🟢 mermaid.test.ts |
| **mermaid SVG DOMPurify 二道防线（F3）** | **P1** | **🔴** SVG 注入前未过 DOMPurify |
| data URI MIME 白名单 | P1 | 🟢 |
| **renderGen 旧结果丢弃（preview.ts:481）** | P0 | **🔴** race 防御无测试 |
| **replaceMermaidBlocks 顺序（preview.ts:481-503）** | P0 | **🔴** 顺序无断言 |

### 模块 4：链接与本地资源（P0）

| 用例 | 级别 | 状态 |
|---|---|---|
| classifyHref 五分类 + 控制字符 | P0 | 🟢 |
| Resolve 八场景 | P0 | 🟢 |
| OpenExternal scheme 白名单 | P0 | 🟢 |
| OpenWithSystem 黑名单 + 常规文件 | P0 | 🟢 |
| ReadAssetDataURL TOCTOU/10MB/.svg 拒 | P0 | 🟢 |
| **AllowSVG 并发读写** | **P1** | **🔴 G2** 加 -race 并发用例 |
| navGuard 404→302/wails 透传 | P0 | 🟢 |

### 模块 5：未保存守卫（P0）

| 用例 | 级别 | 状态 |
|---|---|---|
| SetUnsavedCount + 负数钳位 | P0 | 🟢 |
| beforeClose 三分支 | P0 | 🟢 |
| pendingNotify 补发（多实例） | P0 | 🟢 |
| **dialog returnValue 残留清零（ESC）** | P0 | **🟡** Go 契约已 pin，JS DOM 层无 jsdom 单测 |

### 模块 6：单实例 + 启动文件（P0）

| 用例 | 级别 | 状态 |
|---|---|---|
| extractStartupFiles 旗标/相对/常规 | P1 | 🟢 |
| 队列 FIFO + 并发 | P1 | 🟢 |
| **macOS Finder 双击（OnFileOpen）** | P1 | **🟡 G3** 测试误导，需真实 .exe 落盘 |
| **OnSecondInstanceLaunch wiring** | P1 | **🟡** 通知路径已测，回调绑定未直接断言 |

### 模块 7：配置持久化（P1）

| 用例 | 级别 | 状态 |
|---|---|---|
| ErrCorrupted 自愈 + Mutate | P1 | 🟢 |
| PushRecent 去重/上限 10 | P1 | 🟢 |

### 模块 8：UI/UX（P2）

| 用例 | 级别 | 状态 |
|---|---|---|
| 标题栏拖动/双击/最大化同步 | P2 | 🟢 titlebar.test.ts |
| 主题切换 + mermaid 跟随 | P2 | 🟢 |
| **scheduleSync 代际序** | P2 | **🔴 F4** |
| 字号持久化 | P2 | 🟢 font-size.test.ts（16 用例） |
| **首启读 prefers-color-scheme** | P2 | **🔴 F12** |
| TOC / Sidebar / SplitPane 键盘 | P2 | 🟡 E2E sprint7/8 |
| 复制反馈 / 主题动画时长 | P2 | 🟢/🟡 |

### 模块 9：错误文案契约（P1）

| 用例 | 级别 | 状态 |
|---|---|---|
| **6 处哨兵错误钉死（G9）** | **P1** | **🟡** 已 pin 1 处（ErrExternalModified） |
| **错误码体系替代 includes 匹配（F1）** | **P1** | **🔴** ~10 处 includes 仍耦合 |

### 测试覆盖结论

- **核心业务逻辑**（Go 1/4/5/6/7 + 前端 3）覆盖率 **>85%**，满足 >80% 目标
- **新增缺口**（本轮识别）：G3/G7/G8 三个 P0 级 runtime 无测试守护；F1 错误码化（侵入大但必要）；editor undo 隔离无测试
- **编排层**（main.ts）依赖 DOM + Wails binding，适合 E2E，单测补强收益低

---

## 四、阶段二修复计划（按优先级）

| 序 | 对应问题 | 修复项 | 验证 | 状态 |
|---|---|---|---|---|
| 1 | D1/D2/D3 | 三份文档（TECHNICAL §5.3 / TEST-MATRIX §4 / AUDIT-R1 §三）macOS OnFileOpen 状态统一为 🟢 已实装 + commit 引用 | 人工 diff | 🔄 进行 |
| 2 | AD2/D4 | CHANGELOG [0.2.0] 命令面板 + keyMap 加勘误注脚 | 人工 | 🔄 进行 |
| 3 | D5/AC1 / D6/AC2 | README：5 扩展名关联 + `Ctrl+Shift+F` → `Ctrl+H` | 人工 | 🔄 进行 |
| 4 | AB1/AB2/AB3/AB4/AB5/AB6/D7/D8/D9/D10 | 文档统计 / 表述偏差统一清理 | 人工 + grep | 🔄 进行 |
| 5 | **G1** | fileio error 路径：完整路径走日志，用户可见仅 basename | go test | ⏳ |
| 6 | **G2** | `AllowSVG` 改 `atomic.Bool`，加并发读写 -race 用例 | `go test -race` | ⏳ |
| 7 | **G3** | `extractStartupFiles` 加 Markdown 扩展白名单 + 测试改真实 .exe 落盘 | go test | ⏳ |
| 8 | G4 / G5 | `BrowserOpenURL` 返错上抛；`SaveFile` stat 错误上抛 | go test | ⏳ |
| 9 | G6 | `decodePath` 失败返 `ErrMalformedPath` | go test | ⏳ |
| 10 | G7 | `CopyImageAsset` 补 ≥4 测试 | go test | ⏳ |
| 11 | G8 | `SaveFile` expectMtime 冲突 runtime 补 1 测试 | go test | ⏳ |
| 12 | G9 | 6 处错误文案提 `ErrXxx` 哨兵 + 钉死契约测试 | go test | ⏳ |
| 13 | G10-G15 | P2 清理（OpenDialog All-Files / shutdown 钩子 / 命名常量等） | go test | ⏳ |
| 14 | **F1** | 错误码体系：Go 侧 `FileError` 结构 + 前端 `code` switch 替代 includes | go test + 前端 | ⏳ 需评估侵入 |
| 15 | F2 | `formulaCache` 改 LRU | 前端测试 | ⏳ |
| 16 | **F3** | mermaid SVG DOMPurify 二道防线 | 前端测试 | ⏳ |
| 17 | F4 | `scheduleSync` 加 gen 序 | 前端测试 | ⏳ |
| 18 | F5 | `manualChunks` 改前缀正则 | vite build | ⏳ |
| 19 | F6 | LaTeX 闸门分层文档化 | latex.test.ts 注释 | ⏳ |
| 20 | F7 | `__litemd__bindings` 守 `import.meta.env.DEV` | vite build | ⏳ |
| 21 | F8-F17 | P2 清理（renderChain 截断 / 死代码 / 硬编码 / 防抖 / 常量化等） | 前端 | ⏳ |
| 22 | 测试补强 | editor undo 隔离 / preview renderGen / replaceMermaidBlocks 顺序 / file-ops bind / tabs openInCurrentIfEmpty / sync scroll ping-pong / sidebar/splitpane 单元 | 前端测试 | ⏳ |

**约束遵守**：
- F11 错误码化（14）是侵入式重构，需单独立项并先与用户对齐范围
- F12 navGuard / F8-F16 等 P2 按"无瓶颈不重构"原则，按代价/收益分批
- 测试补强（22）按"硬约束 #4 核心 >80% 覆盖"目标补缺，editor 隔离 / preview renderGen 优先

**阶段二最终验证**：
- `go test ./... -race -count=1` 全绿
- `LITEMD_TEST=all npx tsx src/preview.test-bootstrap.ts` 全绿
- `npx tsc --noEmit` 无错
- `npm run build` 拆包仍生效（main chunk 61KB / gzip 22KB 不退化）
- 修复记录同步至 CHANGELOG.md「[未发布]」段 + ROADMAP 追踪表

---

*审计方法：4 路并行扫描（Go 后端 / 前端 TS / 文档对齐 / 测试矩阵），逐条对照 R1-R8 落地 commit 与新代码，TECHNICAL.md / CHANGELOG.md / ROADMAP / README / TEST-MATRIX 5 份文档逐字交叉验证。*
